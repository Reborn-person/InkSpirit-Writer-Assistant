# 部署更新指南 (2026-02-03) - Token 额度与代付系统

本次更新包含数据库 Schema 变更和新的环境变量配置，请在部署到服务器时严格按照以下步骤操作。

## 1. 数据库迁移 (必须)

本次更新新增了 `UserQuota` 表用于管理用户 Token 配额，必须同步数据库结构。

**操作步骤：**
在服务器项目根目录下运行：
```bash
npx prisma migrate deploy
```
*或者 (如果使用 SQLite 开发环境):*
```bash
npx prisma db push
```

## 2. 环境变量配置 (必须)

新增了平台代付 Key 配置，用于非 PROMAX 用户的 AI 请求。

**操作步骤：**
编辑服务器上的 `.env` 文件，添加以下内容：

```env
# 必填：平台用于代付 Token 的硅基流动 API Key
SILICONFLOW_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

> **注意**：如果不配置此 Key，未填写自定义 Key 的普通用户将无法使用 AI 功能。

## 3. 重启服务

完成以上两步后，请重启应用服务以使配置生效。

```bash
pm2 restart all
# 或者
docker-compose restart
```

## 变更详情

- **新增表**: `UserQuota` (记录每日 Token 使用量)
- **新增接口**: `/api/proxy/chat` (用于处理无 Key 用户的代理请求)
- **逻辑变更**:
  - `lib/ai.ts`: 自动检测 Key 缺失并降级到代理接口。
  - `lib/quota.ts`: 实现了基于等级的配额检查 (PRO: 10w, MAX: 200w)。
